set up cronjob or systemd timer service that runs once a week

$ go run main.go -l 'google scholar' -mark -authors

save this output to a file and then send an email

install mailx with `mailutils` package
